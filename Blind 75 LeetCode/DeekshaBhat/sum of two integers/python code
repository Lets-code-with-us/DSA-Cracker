class Solution(object):
    def getSum(self, a, b):
        while b != 0:
            if b > 0:
                a += 1
                b -= 1
            else:
                b += 1
                a -= 1
        return a

        
